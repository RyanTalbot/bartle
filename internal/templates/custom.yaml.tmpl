style: custom
ai:
  enabled: {{ .AIEnabled }}
  provider: openai
  model: {{ .Model }}
  api_key: {{ .APIKey }}
rules:
  max_line_length: {{ .MaxLen }}
hook:
  auto_apply: {{ .AutoApply }}
  block_on_fail: {{ .BlockOnFail }}
